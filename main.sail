default Order dec
$include <prelude.sail>
$include "prelude.sail"

function fetch_decode_execute() = {
	//
}

function setup(): unit -> unit = {
	//
}

function main(): unit -> unit = {
	setup();
}